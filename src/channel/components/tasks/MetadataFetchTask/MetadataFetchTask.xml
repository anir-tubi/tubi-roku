<?xml version="1.0" encoding="utf-8" ?> 
<!--
 - MetadataFetchTask
 -
 - This is a long-lived task which will multiplex handling
 - of many HTTP requests simultaneously.  We want many
 - requests to happen in parallel but we don't want to
 - let the thread count to get out of control nor incur
 - the overhead of spinning up a new thread for each
 - metadata HTTP request. 
 -
 --> 
<component name="MetadataFetchTask" extends="Task">
    <script type="text/brightscript" uri="pkg:/components/tasks/MetadataFetchTask/MetadataFetchTask.brs" />
    <script type="text/brightscript" uri="pkg:/source/lib/Auth.brs" />
    <script type="text/brightscript" uri="pkg:/source/lib/CmsApi.brs" />
    <script type="text/brightscript" uri="pkg:/source/lib/Log.brs" />
    <script type="text/brightscript" uri="pkg:/source/lib/Request.brs" />
    <script type="text/brightscript" uri="pkg:/source/lib/RequestQueue.brs" />
    <script type="text/brightscript" uri="pkg:/source/lib/TubiBookmarks.brs" />
    <script type="text/brightscript" uri="pkg:/source/lib/TubiNodeHelpers.brs" />
    <script type="text/brightscript" uri="pkg:/source/lib/TubiMetadataTranslate.brs" />
    <script type="text/brightscript" uri="pkg:/source/3rdparty/roku/generalUtils.brs" />
    <script type="text/brightscript" uri="pkg:/components/lib/ExperimentMixin.brs" />
    <script type="text/brightscript" uri="pkg:/source/lib/TubiExperiments.brs" />

    <interface>
      <!-- INPUT -->
        <!--
         - Single request - see MetadataFetchTaskDTO.brs for structure
         -->
        <field id="request" type="assocarray" alwaysNotify="true"/>
      
        <!--
         - Batch request - see MetadataFetchTaskDTO.brs for structure
         -->
        <field id="batchRequest" type="assocarray" alwaysNotify="true"/>

        <!-- 
         - Cancel an outstanding request - see MetadataFetchTaskDTO.brs for structure
         -->
        <field id="cancel" type="assocarray" alwaysNotify="true"/>

      <!-- OUTPUT -->
        <!-- Set to true once the thread loop is up and listening for requests -->
        <field id="ready" type="boolean" value="false" />
    </interface>
</component>
