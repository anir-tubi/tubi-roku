<?xml version="1.0" encoding="utf-8" ?>
<component name="DetailScreen" extends="BaseScreen">

  <script type="text/brightscript" uri="DetailScreen.brs" />
  <script type="text/brightscript" uri="../../lib/CacheValidationMixin.brs" />
  <script type="text/brightscript" uri="../../lib/AnimationMixin.brs" />
  <script type="text/brightscript" uri="../../lib/UserMixin.brs" />
  <script type="text/brightscript" uri="../../../source/lib/TubiNodeHelpers.brs" />
  <script type="text/brightscript" uri="../../../source/lib/Request.brs" />
  <script type="text/brightscript" uri="../../../source/lib/Auth.brs" />
  <script type="text/brightscript" uri="../../../source/lib/TubiTracking.brs" />
  <script type="text/brightscript" uri="pkg:/source/lib/StringUtils.brs" />
  <script type="text/brightscript" uri="../../../source/lib/TimeUtils.brs" />
  <script type="text/brightscript" uri="pkg:/components/lib/FIFAMixin.brs" />

  <interface>
    <!-- INPUTS -->
      <!-- The mode used by info panel, one of: 'category', 'item', 'movie', 'series', 'season', 'episode' -->
      <field id="mode" type="string" alias="DetailInfoPanel.mode" />

      <!-- The availabilityType used by info panel, one of: 'Upcoming', 'Replay', 'Live'-->
      <field id="availabilityType" type="string" />

      <!-- The type of the selectedContentType to show/hide like/dislike CTA-->
      <field id="selectedContentType" type="string" />

      <!-- Indicated whether to show/hide the signIn hint -->
      <field id="needsLoginHint" type="boolean" alias="DetailInfoPanel.needsLogin" />

      <!-- Indicated whether to show/hide the signIn hint -->
      <field id="reminderIsSet" type="boolean" alias="DetailInfoPanel.reminderIsSet" />

      <!-- Indicated whether to show/hide the signIn hint -->
      <field id="infoPanelVisible" type="boolean" alias="DetailInfoPanel.visible" />

      <!-- The following are the strings associated with this UI element -->
      <field id="stringQueueButton" type="string"/>
      <field id="stringNoQueueButton" type="string"/>
      <field id="stringChannelButton" type="string"/>
      <field id="stringNoHistoryButton" type="string"/>
      <field id="stringSignUpButton" type="string"/>
      <field id="stringLikeDislikeButton" type="string"/>
      <field id="stringPlayButton" type="string"/>

      <!-- Pass through signal to the info panel that the height/placement should be recalculated -->
      <field id="calculateInfoHeight" type="boolean" alias="DetailInfoPanel.calculateHeight" />

      <!-- The title displayed on the info panel -->
      <field id="title" type="string" alias="DetailInfoPanel.title" />

      <!-- The title of the episode displayed on the info panel, just below the series title-->
      <field id="episodeTitle" type="string" alias="DetailInfoPanel.episodeTitle" />

      <!-- An assoc array of key/values that populate the items in FirstLineGroup in the DetailInfoPanel -->
      <field id="lineOneData" type="assocarray" alias="DetailInfoPanel.lineOneData" />

      <!-- An assoc array of key/values that populate the items in SecondLineGroup in the DetailInfoPanel -->
      <field id="lineTwoData" type="assocarray" alias="DetailInfoPanel.lineTwoData" />

      <!-- Description of the video displayed on the info panel -->
      <field id="description" type="string" alias="DetailInfoPanel.description" />

      <!-- Array of strings of directors -->
      <field id="directors" type="stringarray" alias="DetailInfoPanel.directors" />

      <!-- Array of strings of actors/actresses -->
      <field id="starring" type="stringarray" alias="DetailInfoPanel.starring" />

      <!-- Length of the video (duration) -->
      <field id="length" type="integer" />

      <!-- Indicates if the video is an episode of a series -->
      <field id="isSeries" type="boolean" value="false" />

      <!-- Indicates whether the user is loggedIn or not -->
      <field id="needsLogin" alias="DetailInfoPanel.needsLogin" />

      <!-- Indicates if the video is in the user's bookmarks/queue -->
      <field id="isBookmark" type="boolean" value="false" />

      <!-- Indicates if the video is in the user's history -->
      <field id="isHistory" type="boolean" />

      <!-- Set the like/dislike state: valid strings are constants.ui.likeDislikeStates.liked or constants.ui.likeDislikeStates.disliked or constants.ui.likeDislikeStates.changing - or it can be an empty string "" to signify none.   -->
      <field id="likeDislikeState" type="string"/>

      <!-- Indicates if the video is in a channel -->
      <field id="isChannelItem" type="boolean" value="false" />

      <!-- The most recently watched point in the video for the user, ie. where the user will resume to -->
      <field id="resumePoint" type="integer" value="0" />

      <!-- Indicates if the content has a trailer -->
      <field id="hasTrailer" type="boolean" value="false" />

      <!-- Indicates if the user is in kidsMode UI or not -->
      <field id="isInKidsMode" type="boolean" value="false" />

      <!-- interface for moving the focus on the details screen menu to a specific item-->
      <field id="jumpToItem" type="integer" alias="Menu.jumpToItem" />

      <!-- informs the details screen if the content controller is waiting for a user category update from the server -->
      <field id="isWaitingForServerResponse" type="boolean" value="false" />

      <!-- hides or shows the loading spinner. Play/Resume button presses are not honored when isLoading = true -->
      <field id="isLoading" type="boolean" alias="DetailSpinner.visible" />

      <!-- Show/hide the related content (You May Also Like) -->
      <field id="showRelated" type="boolean" alias="RelatedContentParentGroup.visible" />

      <!-- Setting to true disallows the Add to My List and Remove from My List buttons from being shown -->
      <field id="disableBookmarks" type="boolean" />

      <!-- holds state info about if there was an error in retrieving the content for this page.
           Is used to prevent multiple navigate_to_page and page_load events if a user retries to load content from the error modal.
       -->
      <field id="contentFetchError" type="boolean" value="false" />

      <!-- content to populated the RelatedGrid -->
      <field id="relatedContent" alias="RelatedGrid.content" />

      <field id="content" type="node" />

      <!-- State that lets the player know how it is being used, so it can determine
        -  how it should do tracking.
        -  Allowed sources are:
        -  'unknown'
        -  'previews'
        -  'automatic'
        -  'deliberate'
        -->
    <field id="playbackSource" type="string"/>

    <!-- Used to store state to determine if we should resume playback at a later time -->
    <field id="shouldResumePlayback" type="boolean" value="false" />

    <!-- OUTPUTS -->

      <!-- Should the full description be displayed -->
      <field id="fullDescriptionSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the suer selects the 'Resume' menu button -->
      <field id="resumeSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user selects the 'Play' menu button -->
      <field id="playSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user selects the 'Like' menu button -->
      <field id="likeSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user selects the 'Dislike' menu button -->
      <field id="dislikeSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user selects the 'Remove Like' menu button -->
      <field id="removeLikeSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user selects the 'Remove Dislike' menu button -->
      <field id="removeDislikeSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user selects the 'Episode List' menu button -->
      <field id="episodeListSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user adds the title to their queue -->
      <field id="addToQueueSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user removes the title from their queue -->
      <field id="removeFromQueueSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user removes the title from their history -->
      <field id="removeFromHistorySelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user wants to see the trailer for the content -->
      <field id="watchTrailerSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user wants to navigate to the channel details screen -->
      <field id="channelSelected" type="boolean" alwaysNotify="true" />

      <!-- set to true when the user wants to sign-up -->
      <field id="signUpButtonSelected" type="boolean" alwaysNotify="true" />

      <!-- REMOVE BELOW CODE ONCE FIFA WORLD CUP IS DONE -->
      <!-- set to true when the user wants to see all game -->
      <field id="seeAllGamesSelected" type="boolean" alwaysNotify="true" />

      <!-- mirrors RelatedGrid.itemSelected, but DetailScreen interjects before sending it to the ContentController -->
      <field id="relatedContentSelected" type="integer" alwaysNotify="true" />

      <!-- set to the contentNode of the RelatedGrid item which was focused when
           the "play" input transport deeplink was triggered
      -->
      <field id="relatedContentToPlay" type="node" alwaysNotify="true"/>

      <!-- tells ContentController/DetailScreenHelpers that the content metadata
           or the related content is out of the cache window and should be updated -->
      <field id="refreshContent" type="boolean" alwaysNotify="true" />

      <!-- set to true when after signup process completed -->
      <field id="removeSignupButton" type="boolean" alwaysNotify="true" />

      <field id="backButtonPressed" type="boolean" alwaysNotify="true" />

      <field id="refreshRelatedContent" type="boolean" alwaysNotify="true" />

      <field id="showRelatedContent" alias="RelatedContentGroup.visible" />

      <!-- Holds a load time start value [returned from UpTime()] that can be used to determine how long the page took to load.
           Used when showing a series/episode page
      -->
      <field id="trackingLoadStartTime" type="integer" />

      <!-- Communicates to the parent that the video preview should be stopped -->
      <field id="stopVideoPreview" type="boolean" alwaysNotify="true"/>

      <!-- The "button_value" for an analytics ButtonComponent -->
      <field id="toggleOnButtonValue" type="string" alwaysNotify="true" />
      <field id="toggleOffButtonValue" type="string" alwaysNotify="true" />
      <field id="confirmButtonValue" type="string" alwaysNotify="true" />

      <!-- Called to report that the Like/Dislike Menu has gained focus or lost focus -->
      <field id="focusOnLikeMenu" type="boolean" value="false"/>


  </interface>

  <children>
    <!-- All the potential menu items, will be dynamically added or removed based on 'content' -->
    <DetailMenuItemContentNode
      id="ResumeMenuItem"
      iconUrl="pkg:/images/icon-resume.webp"/>
    <DetailMenuItemContentNode
      id="PlayMenuItem"
      iconUrl="pkg:/images/icon-play.webp"/>
    <DetailMenuItemContentNode
      id="LikeMenuItem"
      analyticsButtonValue="LIKE"
      isUnfocusedFootprintEnabled="true"
      iconUrl="pkg:/images/icon-like.webp" />
    <DetailMenuItemContentNode
      id="DislikeMenuItem"
      analyticsButtonValue="DISLIKE"
      isUnfocusedFootprintEnabled="true"
      iconUrl="pkg:/images/icon-dislike.webp" />
    <DetailMenuItemContentNode
      id="LikeDislikeMenuItem"
      analyticsButtonValue="LIKE_OR_DISLIKE"
      iconUrl="pkg:/images/icon-like.webp" />
    <DetailMenuItemContentNode
      id="EpisodesMenuItem"
      iconUrl="pkg:/images/icon-all-episodes.webp"/>
    <DetailMenuItemContentNode
      id="AddQueueMenuItem"
      iconUrl="pkg:/images/icon-add-to-queue.webp"/>
    <DetailMenuItemContentNode
      id="RemoveQueueMenuItem"
      iconUrl="pkg:/images/icon-remove-from-queue.webp"/>
    <DetailMenuItemContentNode
      id="RemoveHistoryMenuItem"
      iconUrl="pkg:/images/icon-remove-from-history.webp"/>
    <DetailMenuItemContentNode
      id="ChannelMenuItem"
      iconUrl="pkg:/images/icon-channel.png"/>
    <DetailMenuItemContentNode
      id="WatchTrailerMenuItem"
      iconUrl="pkg:/images/icon-trailer.webp"/>
    <DetailMenuItemContentNode
      id="signUpMenuItem"/>
    <!-- REMOVE BELOW CODE ONCE FIFA WORLD CUP IS DONE -->
    <DetailMenuItemContentNode
      id="SeeAllGamesMenuItem"
      iconUrl="pkg:/images/icon-see-all-games.webp"/>
    <Poster
      id="leftChevron"
      uri="pkg:/images/left-chevron.png"
      width="15"
      height="27"
      translation="[90,528]"/>

    <Spinner
      id="DetailSpinner"
      visible="false"
      width="1920"
      height="1080"
      translation="[0,0]"/>

    <Group
      id="AnimationGroup"
      translation="[0,0]">
      <InfoPanel
        id="DetailInfoPanel"
        mode="item"
        translation="[192,133]"
        maxHeight="433"
        maxTitleLines="2"
        visible="false" />

      <FilledButtonMarkupGrid
        id="Menu"
        translation="[167,642]"
        itemSize="[465,80]"
        itemComponentName="DetailMenuItem"
        itemSpacing="[0,8]"
        numRows="3"
        numColumns="1"
        vertFocusAnimationStyle="floatingFocus"/>

      <FilledButtonMarkupGrid
        id="SecondaryMenu"
        translation="[667,730]"
        itemSize="[465,80]"
        itemComponentName="DetailMenuItem"
        itemSpacing="[0,8]"
        numRows="3"
        numColumns="1"
        visible="false"
        vertFocusAnimationStyle="floatingFocus"/>

      <Group
        id="RelatedContentParentGroup">
        <Group
          id="RelatedContentGroup"
          translation="[192,921]"
          visible="false"
          opacity="0.2">
          <CategoryGridRowLabel
            id="RelatedRowLabel"
            translation="[0,0]">
            <TubiContentNode
              id="RelatedRowLabelContent"
              role="content"/>
          </CategoryGridRowLabel>
          <PosterGrid
            id="RelatedGrid"
            loadingBitmapUri="pkg:/images/placeholder.jpg"
            failedBitmapUri="pkg:/images/placeholder.jpg"
            itemSize="[1920,267]"
            numRows="1"
            basePosterSize="[186,267]"
            itemSpacing="[12,12]"
            fadeFocusFeedbackWhenAutoScrolling="true"
            horizFocusAnimationStyle="fixedFocus"
            focusBitmapUri="pkg:/images/selector-fhd.9.png"
            translation="[0,63]"/>
          <Label
            id="RelatedTitle"
            width="875"
            wrap="false"
            maxLines="1"
            color="0xFFFFFFFF"
            translation="[0,366]">
            <Font
              role="font"
              uri="pkg:/fonts/Vaud-Medium.ttf"
              size="27" />
          </Label>
        </Group>
      </Group>
    </Group>
  </children>
</component>
